// Command unpixel will reconstruct text hidden behind pixelation, a Go port of
// Bishop Fox's Unredacter. This entry point is a placeholder until the port lands.
package main

import "fmt"

func main() {
	fmt.Println("unpixel: not yet implemented")
}
