# Binaries
/unpixel
*.exe
*.test
*.out

# Coverage / test reports
coverage.*
junit.xml

# goreleaser output
/dist/

# SBOM artifact (regenerated by `mise run sbom`)
sbom.cdx.json

# mise local overrides (per-machine, not shared)
mise.local.toml
.mise.local.toml

# OS / editor
.DS_Store
